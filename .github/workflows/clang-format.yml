on: [push]
jobs:
  format-job:
    runs-on: ubuntu-latest
    name: A job to run clang-format on your diff
    steps:
      - name: clang-format-step
        id: clang-format
        uses: blavallee/actions@v0.4
