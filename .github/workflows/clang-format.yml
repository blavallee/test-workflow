on: [push]
jobs:
  format-job:
    runs-on: ubuntu-latest
    name: A job to run clang-format on your diff
    steps:
      - uses: actions/checkout@v2.3.3
        with:
          fetch-depth: 2
      - uses: blavallee/actions@v0.8 
