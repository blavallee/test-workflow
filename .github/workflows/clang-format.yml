on:
  pull_request:
    types: [assigned, opened, synchronize, reopened]
jobs:
  format-changeset:
    runs-on: ubuntu-20.04 
    name: A job to run clang-format on your diff
    steps:
      - name: Checkout
        uses: actions/checkout@v2.3.3
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha }}
      - name: 'Run clang format'
        id: format
        run: |
          sudo apt-get update  && sudo apt-get install --no-install-recommends --assume-yes clang-format
          git diff -U0 --no-color origin/master HEAD | /usr/share/clang/clang-format-10/clang-format-diff.py -i -p1
          if [ -n "$(git status --porcelain)" ]; then
            echo "::set-output name=changes_detected::true";
          else
            echo "::set-output name=changes_detected::false";
          fi
      - name: 'Commit changes'
        id: commit
        if: steps.format.outputs.changes_detected == 'true'
        run: |
          new_branch_suffix="${{github.head_ref}}-$(git rev-parse HEAD)"
          git checkout -b "clang-format-$new_branch_suffix"
          git config user.name coveobot
          git config user.email coveobot@coveo.com
          git commit --all --message "Clang format on $new_branch_suffix"
          git push origin "clang-format-$new_branch_suffix"
          echo "::set-output name=new_branch::clang-format-$new_branch_suffix"
      - name: Open pull request
        if: steps.format.outputs.changes_detected == 'true'
        uses: actions/github-script@v3.1.0
        with:
          script: |
            console.log(context)
            github.pulls.create({
              owner: 'blavallee',
              repo: 'test-workflow',
              head: '${{steps.commit.outputs.new_branch}}',
              base: '${{github.head_ref}}',
              title: 'Apply clang-format'
            })
      - name: Set the state
        if: steps.format.outputs.changes_detected == 'true'
        run: exit 1

